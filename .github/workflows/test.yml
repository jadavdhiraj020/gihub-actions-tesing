name: Jobs Demo

on: [push]

jobs:
  build:
    needs: config
    runs-on: ubuntu-latest
    steps:
      - run: echo "Building..."

  config:
    name: config
    runs-on: ubuntu-latest
    steps:
      - run: echo "config..."

  test:
    name: Test Step
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Testing..."

  deploy:
    name: deployed
    needs: build
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployed started..."
